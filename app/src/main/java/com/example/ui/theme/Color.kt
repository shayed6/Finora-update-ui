package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// ==========================================
// Finora Official Brand & Theme Color Tokens
// ==========================================

// Primary Palette
val FinoraNavy = Color(0xFF0B1F4B)           // Primary variant / headers / text primary
val PrimaryBlue = Color(0xFF1769FF)          // Primary: buttons, active states, links
val PrimaryBlueDark = Color(0xFF0B1F4B)      // Pressed button state: darken to Navy
val PrimaryBlueLight = Color(0xFF4C8DFF)     // Lighter blue for dark theme contrast

// Success / Growth Accent
val GrowthGreen = Color(0xFF19C77A)          // Positive results, gains, growth indicators
val GrowthGreenLight = Color(0xFFE8F9F1)     // Soft green tint background for result cards
val GrowthGreenDark = Color(0xFF119559)      // Supporting note text in darker green tone
val GrowthGreenDarkTheme = Color(0xFF2FE08F) // High-contrast green for dark theme

// Error / Alert
val AlertRed = Color(0xFFEF5350)             // Losses, warnings, validation errors
val AlertRedLight = Color(0xFFFDECEC)        // Soft red tint background for warning result cards
val AlertRedDark = Color(0xFFC62828)         // Supporting note text in darker red tone
val AlertRedDarkTheme = Color(0xFFFF6B66)    // High-contrast red for dark theme

// Light Theme Tokens
val BackgroundLight = Color(0xFFF7F9FC)      // Soft neutral canvas background #F7F9FC
val SurfaceLight = Color(0xFFFFFFFF)         // Cards, modals, sheets
val BorderSubtle = Color(0xFFE3E8F2)         // Flat border & dividers
val TextPrimaryLight = Color(0xFF0B1F4B)     // Text primary
val TextSecondaryLight = Color(0xFF5C6B8A)   // Text secondary / muted

// Dark Theme Tokens (Optional / Nice-to-have)
val BackgroundDark = Color(0xFF08111F)
val SurfaceDark = Color(0xFF101B33)
val BorderSubtleDark = Color(0xFF223252)
val TextPrimaryDark = Color(0xFFF2F5FA)
val TextSecondaryDark = Color(0xFF8B9AC0)


